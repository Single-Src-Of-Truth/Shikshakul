import { Route } from '@angular/router';
import { FeatureTimetableComponent } from './feature-timetable/feature-timetable.component';

export const featureTimetableRoutes: Route[] = [
  { path: '', component: FeatureTimetableComponent },
];
