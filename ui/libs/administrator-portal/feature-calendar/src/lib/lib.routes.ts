import { Route } from '@angular/router';
import { CalendarPageComponent } from './calendar-page/calendar-page.component';

export const featureCalendarRoutes: Route[] = [
  { path: '', component: CalendarPageComponent },
];
